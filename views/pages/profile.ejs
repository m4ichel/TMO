<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - Task Matrix Organizer</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Oxanium:wght@400;500;700&display=swap" rel="stylesheet">
    <!-- Add Lucide script -->
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>
</head>

<body>
    <div class="container">
    <div class="sidebar">
        <a href="#" class="nav-item" id="sidebar-toggle">
            <div class="nav-icon">
                <i data-lucide="menu" class="lucide-icon"></i>
            </div>
        </a>
        <a href="/home" class="nav-item">
            <div class="nav-icon">
                <i data-lucide="home" class="lucide-icon"></i>
            </div>
        </a>
        <a href="/profile" class="nav-item active">
            <div class="nav-icon">
                <i data-lucide="user" class="lucide-icon"></i>
            </div>
        </a>
    </div>
    <div class="main-content">
        <div class="profile-container">
            <div class="profile-header">
                <div class="profile-info">
                    <div class="title-large"><%= user.username %></div>
                    <div class="title-medium">Created: <%= createdAt %></div>
                </div>
                <img class="profile-avatar" src="/images/default-profile.png" alt="Profile avatar" />
            </div>
            
            <div class="profile-areas">
                <div class="title-large"><%=user.username %>'s public areas:</div>
                <div class="grid-container">
                    <% if (areas.length < 1) { %>
                        <div class="no-areas-message">No public areas found.</div>
                    <% } else { %>
                        <% areas.forEach(area => { %>
                            <a href="/area/<%= area.id %>" class="card-link">
                                <div class="card">
                                    <img class="card-image" src="https://placehold.co/277x180" alt="Area thumbnail" />
                                    <div class="card-content">
                                        <div class="card-title"><%= area.title %></div>
                                        <div class="card-date">Created at: <%= new Date(area.created_at).toLocaleDateString() %></div>
                                    </div>
                                </div>
                            </a>
                        <% }) %>
                    <% } %>
                </div>
            </div>
        </div>
        
        <div class="footer">
            <div><span class="footer-text footer-text-bold">did you know?</span><span class="footer-text footer-text-regular"> → TMO kinda looks like a face if you look hard enough.</span></div>
        </div>
    </div>
</div>

<script src="/js/sidebar.js"></script>
<script src="/js/icons.js"></script>
</body>

</html>
