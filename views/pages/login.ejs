<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Task Matrix Organizer</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Oxanium:wght@400;500;700&display=swap" rel="stylesheet">
    <!-- Add Lucide script -->
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>
</head>

<body>
    <div class="login-container">
        <div class="login-box">
            <form action="/login" method="POST" class="login-form">
                <div class="tmo-logo-container">
                    <div class="tmo-logo">
                        <div class="tmo-letter" style="color: var(--color-text-light);">T</div>
                        <div class="tmo-letter" style="color: var(--color-text-light);">M</div>
                        <div class="tmo-letter" style="color: var(--color-text-light);">O</div>
                    </div>
                </div>
                
                <div class="login-fields">
                    <div class="input-field">
                        <div class="input-icon">
                            <i data-lucide="user" class="lucide-icon"></i>
                        </div>
                        <input class="input-text" type="text" id="username" name="username" placeholder="Username" required>
                    </div>
                    
                    <div class="input-field">
                        <div class="input-icon">
                            <i data-lucide="lock" class="lucide-icon"></i>
                        </div>
                        <input class="input-text" type="password" id="password" name="password" placeholder="Password" required>
                    </div>
                    
                    <% if (error) { %>
                        <div class="error-message">
                            <%= error %>
                        </div>
                    <% } %>
                </div>
                
                <button type="submit" class="btn-primary">
                    <span class="btn-text">Login</span>
                </button>
            </form>
            
            <div class="login-footer">
                <p class="text-regular">New user? <a href="/register" class="auth-link">Register here</a></p>
            </div>
        </div>
    </div>
    <script src="/js/icons.js"></script>
</body>

</html>
